#pragma once

#include <core/types.hpp>

namespace smo::network::tcp {

// TCP listener — accepts inbound connections on a bound port.
// Full implementation in Sprint 4.
// Presently delegates to core/transport/tcp_transport.hpp.

} // namespace smo::network::tcp
