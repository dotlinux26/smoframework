#pragma once

#include <core/types.hpp>

namespace smo::network::udp {

// UDP discovery — stateless node discovery via UDP broadcast / multicast.
// Used for LAN mesh bootstrap (no seed required).
// Full implementation in Sprint 4.

} // namespace smo::network::udp
