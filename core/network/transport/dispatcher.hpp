#pragma once

#include <core/types.hpp>

namespace smo::network::transport {

// Transport dispatcher — routes outbound messages to correct transport.
// Protocol layer (Discovery/Control/Execution/Data) → transport selector.
// Full implementation in Sprint 4.

} // namespace smo::network::transport
